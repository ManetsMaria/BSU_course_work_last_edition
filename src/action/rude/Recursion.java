package action.rude;

import structure.coveringarrays.CoveringArrays;
import structure.graph.Element;
import structure.graph.Graph;

public class Recursion {

    int size;

    /*public CoveringArrays makeRecursive(Graph graph){
        size = graph.getSize();

    }

    private CoveringArrays makeRecursive(Element element){
        if (el)
    }*/
}
